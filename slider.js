let prevArrow = document.getElementById('prevArrow')
let nextArrow = document.getElementById('nextArrow')
$('.doctor').slick({
    infinite: true,
    slidesToShow: 4,
    slidesToScroll: 1,
    prevArrow: prevArrow,
    nextArrow: nextArrow
  }); 


$('.department').slick({
    infinite: true,
    slidesToShow: 4,
    slidesToScroll: 1,
    prevArrow: prevArrow,
    nextArrow: nextArrow
  }); 